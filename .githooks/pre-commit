#!/bin/sh
# Recarimba o index.html quando um asset carimbado entra no commit.
# Ative uma vez com:  git config core.hooksPath .githooks

if git diff --cached --name-only | grep -qE '^(js/|css/|manifest\.json$)'; then
  node scripts/stamp-assets.js || exit 1
  git add index.html
fi
